#!/bin/sh

set -e

umask 077

basedir=~/.SKOIN
dbfile="$basedir/DB_CONFIG"
cfgfile="$basedir/SKOIN.conf"

[ -e "$basedir" ] || mkdir "$basedir"

# SKOIN does not clean up DB log files by default
[ -e "$dbfile" ] || echo 'set_flags DB_LOG_AUTOREMOVE' > "$dbfile"

exec /usr/lib/SKOIN/SKOIN-qt "$@"
